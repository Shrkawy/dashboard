// import { useState, useEffect, useContext } from "react";
import { Paper } from "@material-ui/core";
// import { AuthContext } from "../context";
// import { useHttpClint } from "../hooks/send-request";

export default function ItemDialog({ itemId, itemType }) {
//   const [item, setItem] = useState(null);
//   const { userId } = useContext(AuthContext);

//   const { error, isLoading, sendReuest } = useHttpClint();

//   let url;
//   switch(itemType) {
//       case 'customer': return url = `/`
//   }

//   useEffect(() => {
//     const getItem = async () => {
//       try {
//         const res = await sendReuest("get", `/${userId}${url}`);
//       } catch (err) {}
//     };
//     getItem();
//   // eslint-disable-next-line react-hooks/exhaustive-deps
//   }, []);
  return <Paper></Paper>;
}
